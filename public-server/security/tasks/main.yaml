---
# Main task file for security baseline role

- name: Include unattended upgrades configuration
  ansible.builtin.include_tasks: unattended-upgrades.yml
  tags:
    - security
    - unattended-upgrades

- name: Include sshd configuration
  ansible.builtin.include_tasks: sshd.yml
  tags:
    - security
    - sshd
